# Django项目配置示例
# 复制此文件为 .env 并根据需要修改

# 是否启用异步服务 (Redis + Celery)
# 默认: false (不启用)
# 设置为 true 时，会在启动时同时启动 Redis 和 Celery worker
# 适用场景: 需要使用异步任务、定时任务时启用
ENABLE_ASYNC_SERVICES=false

# Redis 服务器地址 (当启用异步服务时生效)
REDIS_SERVER_URL=127.0.0.1

# Django 运行配置
DJANGO_RUNSERVER_HOST=0.0.0.0
DJANGO_RUNSERVER_PORT=8080
